package com.learntogether.learntogether.Enum;
public enum PoolStatus {
    ACTIVE, // assigned to the pool as soon as it is created
    INACTIVE // if no post is posted on a pool for a month then it is made readonly
}
